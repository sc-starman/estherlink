Place `sqlite3.exe` in this directory for bundled local OmniPanel/runtime use.

Expected path at runtime:

- `Service/sqlite3/sqlite3.exe`

The service injects `OMNIRELAY_SQLITE3_BIN` for local relay OmniPanel processes when this file exists.
