@page "/account/register"
@model EstherLink.Backend.Pages.Account.RegisterModel
@{
    ViewData["Title"] = "Register";
}

<div class="max-w-md mx-auto px-4 py-16">
    <h1 class="text-3xl font-bold mb-6">Create your OmniRelay account</h1>

    @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
    {
        <div class="mb-4 rounded border border-red-500/50 bg-red-500/10 p-3 text-red-200">@Model.ErrorMessage</div>
    }

    <form id="registerForm" method="post" class="space-y-4 rounded-xl border border-slate-800 bg-slate-900 p-6">
        @Html.AntiForgeryToken()
        <input asp-for="Input.RecaptchaToken" type="hidden" id="registerRecaptchaToken" />
        <div>
            <label asp-for="Input.Email" class="block text-sm mb-1">Email</label>
            <input asp-for="Input.Email" class="w-full rounded bg-slate-800 border border-slate-700 px-3 py-2" />
            <span asp-validation-for="Input.Email" class="text-red-300 text-xs"></span>
        </div>
        <div>
            <label asp-for="Input.Password" class="block text-sm mb-1">Password</label>
            <input asp-for="Input.Password" class="w-full rounded bg-slate-800 border border-slate-700 px-3 py-2" />
            <span asp-validation-for="Input.Password" class="text-red-300 text-xs"></span>
        </div>
        <div>
            <label asp-for="Input.ConfirmPassword" class="block text-sm mb-1">Confirm Password</label>
            <input asp-for="Input.ConfirmPassword" class="w-full rounded bg-slate-800 border border-slate-700 px-3 py-2" />
            <span asp-validation-for="Input.ConfirmPassword" class="text-red-300 text-xs"></span>
        </div>

        <button type="submit" class="w-full rounded bg-cyan-500 text-slate-900 font-semibold px-4 py-2 hover:bg-cyan-400">Register</button>
    </form>

    <p class="mt-4 text-sm text-slate-400">Already have an account? <a href="/account/login" class="text-cyan-300">Login</a></p>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    @if (Model.RecaptchaEnabled && !string.IsNullOrWhiteSpace(Model.RecaptchaSiteKey))
    {
        <script src="https://www.google.com/recaptcha/api.js?render=@Model.RecaptchaSiteKey"></script>
        <script>
            (function () {
                const form = document.getElementById("registerForm");
                if (!form || !window.grecaptcha) return;

                form.addEventListener("submit", function (event) {
                    if (form.dataset.recaptchaReady === "true") return;
                    event.preventDefault();

                    grecaptcha.ready(function () {
                        grecaptcha.execute("@Model.RecaptchaSiteKey", { action: "register_form" })
                            .then(function (token) {
                                document.getElementById("registerRecaptchaToken").value = token || "";
                                form.dataset.recaptchaReady = "true";
                                form.submit();
                            });
                    });
                });
            })();
        </script>
    }
}
